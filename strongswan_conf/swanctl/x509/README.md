# swanctl/x509 目录说明

这个目录用于存放 X.509 证书文件。

## 需要迁移的文件：

1. **caCert.pem** - CA 证书
2. **serverCert.pem** - 服务器证书
3. **caCert3.pem** - 第二个 CA 证书
4. **serverCert3.pem** - 第二个服务器证书

## 证书文件说明：

- 这些证书文件应该从原来的配置目录复制到这里
- 确保证书文件权限设置为 644
- 证书文件应该是 PEM 格式
