# swanctl/private 目录说明

这个目录用于存放私钥文件。

## 需要迁移的文件：

1. **serverKey.pem** - 服务器私钥
   - 从原来的 ipsec.secrets 中的 `: RSA serverKey.pem` 引用
   - 请将实际的私钥文件复制到这里

2. **其他私钥文件**
   - 如果有客户端私钥，也需要放在这里

## 安全注意事项：

- 确保私钥文件权限设置为 600
- 只有 root 用户可以访问这些文件
- 不要将私钥文件提交到版本控制系统
